Pointers
* Memo allocation
* Arrays and pointers
* Double pointers
* Passing pointer in Functions


Memory allocations
* Each byte has address
* Printing address => Hexadecimal number
	* a variable would have multiple bytes, so would technically have multiple address
	* however the address of a variable is the address of the first byte   
	* Getting address
		=> &varible_name

Pointers
* Storing the address of the variable
	int x;
	int *p_x;
	p_x = &x;
	value at => *

Arrays Pointers
	* memory allocation is continuous
	* the variable itself is a pointer

Double Pointer
* stores the address of a pointer

Passing pointer in the function

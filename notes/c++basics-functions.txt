Functions
	Definition
		Return type
		Function Name
		Parameters
	Purpose 
		Code Reuse

main
	return 0 is successful
		Before there was compilation error
		Newer compilers doesn't required return 0
	return 1 is failed


Pointers and References
	By default the copy of the variable is passed (Pass by value)
	Use references 
		void increment(int &n){
			...	
		}

In built functions
	swap(a, b)
	max(a, b)
	min

Only arrays pass by reference
	not possible to change what an array reference refers to after it has been initialized

Multidimentional arrays require array size to be4 passed




21:40


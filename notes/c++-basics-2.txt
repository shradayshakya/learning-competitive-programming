Conditional Statements
* Relational Operators 
	== 
	!=
* Multiple Statements
	&&
	||
* IF conditions
	if (condiion) {
		...
	} 
	else if (condition) {

	} else {
		...
	}	

* Scopes
	{ } => Creates new scope
	global variables => outside the main method

* Loops
	while loop 
		while (condition) {

		}

	for loop
		for (initialization; conditions; operations) {

		}

* Jump Statements
	break;
		last for loop
	continue;
		last for loop

* Memory Structure
	Kernal Space (Code cannot access this)
	Stack
		local variables
		around 
	Memory Segment
		File mappings 
		Anonymous segments
	Heap
	BSS Segment
		Uninitialized static variables
	Data Segment
		Static variables initiatized by the programmer
	Text Segment
		Binary image of the process

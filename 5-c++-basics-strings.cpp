#include<bits/stdc++.h>

using namespace std;

int main() {
	// string str = "Hello";
	// cout << str[str.length() - 1];


	// string numberInLines;
	// getline(cin, numberInLines);
	// cout << numberInLines << endl;

	int t;
	cin >> t;
	cin.ignore();
	while (t--) {
		string line;
		getline(cin, line);
		cout << line << endl;
	}
}